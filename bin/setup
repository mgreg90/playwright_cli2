#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

bundle install

# Do any other automated setup that you need to do here
# 
# build playwright files

mkdir -p $HOME/.playwright/bin $HOME/.playwright/plays

export PATH=$PATH:$HOME/.playwright/bin